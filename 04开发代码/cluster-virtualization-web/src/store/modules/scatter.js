const state = {};

const mutations = {
    addData(state, data) {

    }

};

const actions = {
    addData({commit}, data) {
        commit('addData', data)
    }
};

const getters = {};


export default {
    state,
    mutations,
    getters,
    actions
}